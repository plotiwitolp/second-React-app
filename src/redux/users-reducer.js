const initialState = {
        users: [
            {id: 1, name: "User 1",},
            {id: 2, name: "User 2",},
        ]
}

const usersReducer = (state = initialState, action) => {

    return state
}
export default usersReducer